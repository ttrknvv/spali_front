export enum Accesses {
  GRANT_ACCESS = 'GRANT_ACCESS',
}

export enum Components {
  CATALOG = 'CATALOG',
}

export enum Actions {
  READ = 'READ', // Просмотр, поиск, просмотр списка
  WRITE = 'WRITE', // Создание, редактирование, удаление
}
